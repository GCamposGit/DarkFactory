# HF-09-01 — Binding de agentes e revisão por etapa

Versão 1.0 · origem user-demand · parent `HF-09` · prioridade P0.
Baseline: `83e5298eb231599076811802dceac8575c7f6feb`. Plano: `waiting_dependency`; design: `design_specified`; implementação não iniciada; operação não verificada.

Contrato comum obrigatório: [CONTRACTS.md](CONTRACTS.md). [Plano mestre](../../CONTINUOUS_AUTONOMY_PLAN_2026-09-18.md).

## Objetivo, APIs e ordem

Fechar service/method/signature/conversion e schemas por estágio; mapear AgentExecutor.start/resume e ImplementationCycleService; tools/prompts/worktree versionados e authority do VerificationContext.

Ordem: inputs e preflight/coleta → teste novo/contraprova → implementação ou binding → focal → gates → revisão → integração → evento sucessor.

Dependências: HF-07-01, HF-05-04. Papel `high_architecture`; ambiente `executor-binding`.
Binding: contrato desta unidade. Inputs: handoff, CONTRACTS.md, plan.json, hashes da baseline e outputs versionados dos predecessores. Nunca usar digest divergente do aprovado.

## Ownership e outputs

- `docs/handoffs/continuous-autonomy/bindings/HANDLERS.md` — NOVO, a criar.
- `.factory/planning/continuous-autonomy/bindings/handlers.json` — NOVO, a criar.

Demais paths são somente leitura; logs/relatório exclusivos do ticket são auxiliares conforme contrato comum. Outputs vinculados ao SHA/plan_digest. Non-goals: implementar predecessores, mudar critérios, comprar créditos, converter checkpoints, editar outros projetos ou declarar operação por teste local.

## Aceite e exemplos

Smoke com edição e teste real; só texto não qualifica. Sujeito revisor distinto. Adapter ausente gera leaf de implementação específica com APIs resolvidas.

Válido: input da versão atual, dependências verificadas e owner/fencing corrente produzem a saída contratada e um evento sucessor. Inválido: mesma chave com input/digest diferente, receipt antigo ou claim vencido falha sem efeito. As contraprovas específicas acima são obrigatórias além destas invariantes.

## Validação e continuidade

`VALIDATE_CMD`: `python .factory/planning/continuous-autonomy/verify.py --binding HF-09-01`.
Arquivos de binding/resultado NOVOS. Comando estrutural só passa quando existirem e forem não vazios; não substitui revisão semântica high e probes/ensaio acima. Não foi executado como aceite nesta sessão.
Depois executar gates existentes de CONTRACTS.md sequencialmente; coletar stdout/exit, contagens, candidate SHA, identity/route/environment e contraprova. Revisão independente é posterior por sujeito distinto; esta especificação não é seu recibo.

Timeout/retry/heartbeat/fencing/idempotência: CONTRACTS.md; binding 5 min, job 30 min, release 15 min, ensaio 24 h, heartbeat 10 s e lease 45 s. Sem teto financeiro novo. Retomada consulta resultado/operação persistida e revalida lease antes de retry. Humano só para intenção, acesso, segredo ou aceite ainda não concedido, após alternativas; arquitetura volta ao high.
Sucessores automáticos: HF-09-02, HF-25-01. Conclusão grava resultado+outbox; reconciliador recupera evento perdido e só libera quando todas dependências estiverem satisfeitas.
Rollback: revert seletivo/desativar nova versão, preservar ledger/logs e owners. Efeito externo exige lookup/rollback do binding; sem exclusão cega. Lacuna técnica retorna ao planejador high.
