# HF-13 — DarkHub: Visibilidade, Filas & Ingestão HF/INFRA

Handoff executável do ticket HF-13.

## Identidade e escopo

- `ticket_id`: `HF-13`
- `parent_id`: `HF-13`
- `status`: `COMPLETED`
- `origem`: `user-demand` (Trilha do DarkHub: Visibilidade & Filas)
- `módulos`: `hub.backend`, `hub.frontend`, `core.roadmap`

## Objetivos e Entregas

1. **Diagnóstico e Blindagem 404 em `/api/tasks/dashboard`**:
   - Aliases raiz e variantes com trailing slash: `/tasks/dashboard`, `/tasks/dashboard/`, `/api/tasks/dashboard/`, `/tasks`, `/tasks/`, `/api/tasks/`.
   - Fallback resiliente no frontend `hub/frontend/tasks.js`.
   - Remoção do bloqueio operacional de reinício do Hub oriundo do congelamento histórico em HF-01.

2. **Ingestão Canônica HF e INFRA**:
   - `HybridWorkflowPlanSource`: parsing de ondas 1 e 2 de `docs/HYBRID_WORKFLOW_PLAN_2026-09-08.md`, resolução de dependências e vínculo com relatórios de evidência em `.factory/reports/hf-*-report.md`.
   - `InfraRoadmapJsonSource`: leitura estruturada de `.factory/infra/roadmap.json` e pré-requisitos de `.factory/infra/roadmap.md`.
   - Ingestão ativada no `HubService` para compor o cockpit central de visibilidade da fábrica autônoma.

3. **Validação e Portões**:
   - Testes unitários e de integração em `tests/test_hub_hf13.py`, `tests/test_task_dashboard.py`, `tests/test_roadmap.py` e suíte geral do DarkFac.
