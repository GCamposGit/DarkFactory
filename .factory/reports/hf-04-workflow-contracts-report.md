# HF-04 — workflow contracts and readiness gate report

## Handoff

- Ticket: `HF-04-01` (local contract slice of HF-04)
- Parent: `HF-04`
- Origin: `user-demand`
- Branch: `codex/hf-01-baseline`
- Base SHA observed: `af1c28df9b301f7f5adff49da9b6ec7919b514d3`
- Final SHA: uncommitted; `.git` is read-only in this workspace
- Scope: shared `core/` only; Canaletto untouched
- External access: no PostgreSQL, DBOS, cloud service or secret used

## Delivered

- Added strict, JSON-serializable HF-04 contracts in `core/workflow/contracts.py`:
  `GrillRecord`, environment manifest/evidence, secret references, manual
  dependencies, workflow/readiness states and complete `WorkflowHandoff`.
- Added pure readiness logic in `core/workflow/readiness.py`: closed state
  transitions, environment/evidence matching, freshness and result checks,
  unresolved-human-dependency blocking, simulation-vs-target separation and
  fail-closed `mark_delivered`.
- Added package exports in `core/workflow/__init__.py`.
- Added 13 deterministic contract/gate tests in
  `tests/test_workflow_contracts.py`.
- Added the executable handoff in `docs/handoffs/HF-04.md`.

## Validation evidence

| Command | Result |
| --- | --- |
| `python core/harness/terminal_env.py --check` | `[TERMINAL_ENV_PASS]` |
| `python -m pytest tests/test_workflow_contracts.py -q` | 13 passed |
| `python core/harness/runner.py --quick` | `[HARNESS_PASS]`, 490 collected, 488 passed, 2 skipped |
| `python -m pytest tests -v --ignore=tests/test_canaletto.py` | 490 collected, 488 passed, 2 skipped |
| `python -m core.learning.cli benchmark` | 4/4 passed, 100% |
| `python -m compileall -q core/workflow tests/test_workflow_contracts.py` | exit 0 |
| `python -c "import core.workflow"` | `[IMPORT_PASS]` |

The two existing skips are the live-audio probe and the Windows symlink probe.
The required suite did not execute `tests/test_canaletto.py`; no Canaletto file
appears in the changed-path check.

## RCA applied

1. Initial focal run rejected string enum values because `strict=True` was set on
   the model base. The RCA removed global strict coercion while retaining
   `extra="forbid"`, closed enums, literals and content validators so JSON
   payloads remain usable.
2. The gate referenced `WorkflowState.READY_FOR_RELEASE`, which belongs to the
   readiness vocabulary. The RCA separated workflow-state and readiness-state
   comparisons.

Both fixes were followed by a green focal test and final harness run.

## Residual state

- HF-02-05/07 remain `waiting_access` for authorized disposable PostgreSQL;
  this ticket does not unblock or simulate them.
- No commit, push, PR, merge or remote SHA verification was possible because
  `.git` is read-only. Delivery is therefore local and uncommitted, not a
  remotely integrated ticket.
- Existing HF-02 untracked files and `.factory/baselines/` were preserved.
- `.factory/learning/learning_ledger.json` contains the session learning/RCA
  update generated by the continuous-improvement protocol.
